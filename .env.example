# Backend conspect-service (Spring Boot). The frontend talks only to this URL;
# file uploads are forwarded by Java to conspect-ai, and the result is delivered
# back via callback. The frontend polls /api/v1/conspects/{id} while is_generating=true.
VITE_CONSPECT_API_URL=http://localhost:8090
